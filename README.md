# Wish Map Bot - Kolors MVP

Telegram-бот для создания персональной карты желаний с использованием Kolors (Kling Image) API. Пользователь отправляет селфи, вводит 3-9 желаний, выбирает формат, и получает красивую карту с визуализацией каждого желания.

## Возможности

- ✅ **Kolors API Integration**: Использует Kling Image для генерации реалистичных изображений
- ✅ **Селфи в сценах**: Ваше лицо естественно интегрируется в каждую сцену желания
- ✅ **3 формата вывода**: Phone Wallpaper, Computer Wallpaper, Print A4
- ✅ **Адаптивная сетка**: Автоматически подстраивается под количество желаний (3-9)
- ✅ **Асинхронная обработка**: Быстрая генерация нескольких изображений параллельно
- ✅ **Graceful Fallback**: Всегда возвращает результат, даже при ошибках

## Форматы вывода

| Формат | Размеры | Описание |
|--------|---------|----------|
| 📱 Phone Wallpaper | 1080 × 1920 px | Вертикальный формат для телефона |
| 💻 Computer Wallpaper | 1920 × 1080 px | Горизонтальный формат для компьютера |
| 🖨️ Print A4 | 2480 × 3508 px | Вертикальный формат для печати A4 (300 dpi) |

## Структура проекта

```
/app
  /api
    main.py              # FastAPI приложение
    /routes
      assemble_map.py    # Эндпоинт для сборки карты
  /services
    kolors_client.py     # Клиент для Kolors API
    map_assembler.py      # Сборка финальной карты
  /bot
    bot.py               # Запуск Telegram-бота
    dialog.py            # Состояния диалога
    handlers.py          # Обработчики команд
  /utils
    formats.py           # Определения форматов
    grid.py              # Расчёт сетки
    images.py            # Утилиты для работы с изображениями
    storage.py           # Работа с временными файлами
  config.py              # Конфигурация
.env.example
requirements.txt
README.md
```

## Быстрый старт

### 1. Установка зависимостей

```powershell
# Создайте виртуальное окружение
python -m venv .venv
.venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка

Создайте файл `.env` на основе `.env.example`:

```env
# Telegram Bot
BOT_TOKEN=your_telegram_bot_token_here

# Backend
BACKEND_URL=http://localhost:8000
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# Kolors API
KOLORS_API_URL=https://api.gen-api.ru/api/v1/networks/kling-image
KOLORS_API_KEY=your_kolors_api_key_here
```

**Важно**: Получите API ключ Kolors на [gen-api.ru](https://gen-api.ru)

### 3. Запуск

**Терминал 1 — Backend:**
```powershell
cd app/api
python main.py
# или
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

**Терминал 2 — Bot:**
```powershell
cd app/bot
python bot.py
```

### 4. Использование

1. Откройте Telegram и найдите вашего бота
2. Отправьте `/start`
3. Отправьте своё селфи (фото лица)
4. Выберите формат карты (phone/pc/a4)
5. Введите от 3 до 9 желаний (каждое отдельным сообщением)
6. Напишите `ГОТОВО`, чтобы завершить
7. Дождитесь генерации (5-10 минут)
8. Получите готовую карту желаний!

## Как работает система

### Поток обработки

1. **Пользователь отправляет селфи** → Бот сохраняет фото и получает URL
2. **Пользователь выбирает формат** → Определяются размеры выходного изображения
3. **Пользователь вводит желания** → Собирается список из 3-9 желаний
4. **Генерация изображений** → Для каждого желания отправляется запрос в Kolors API:
   - Используется промпт с интеграцией лица из селфи
   - Сохраняется естественный вид лица, возраст, пол, этническая принадлежность
   - Сцена соответствует теме желания
5. **Сборка карты** → Все сгенерированные изображения собираются в сетку:
   - Адаптивная сетка (3×1, 2×2, 2×3, 3×3)
   - Добавляется заголовок "Wish Map 2026"
   - Добавляются подписи к каждому изображению
6. **Возврат результата** → Пользователь получает финальную карту

### Kolors API Integration

Система использует Kolors (Kling Image) API для генерации:

- **Модель**: `kling-v1`
- **Image Fidelity**: 1.0 (максимальное сохранение лица)
- **Аспектное соотношение**: Автоматически определяется по формату
- **Промпт**: Оптимизирован для сохранения лица и создания реалистичных сцен
- **Negative Prompt**: Исключает искажения и артефакты

### Graceful Fallback

- Если генерация изображения не удалась → создаётся placeholder с текстом желания
- Если сборка сетки не удалась → создаётся простая однокадровая карта
- Все ошибки логируются, но не прерывают выполнение
- Пользователь всегда получает валидную карту

### Адаптивная сетка

- **3 желания** → 3×1 (горизонтальная линия)
- **4 желания** → 2×2 (квадрат)
- **5-6 желаний** → 2×3 (две строки)
- **7-9 желаний** → 3×3 (квадратная сетка)

## Технические детали

### Зависимости

- **aiogram 3.4.1** — Telegram Bot API
- **fastapi + uvicorn** — Backend API
- **httpx** — Асинхронные HTTP запросы к Kolors API
- **Pillow** — Обработка изображений и сборка сетки
- **loguru** — Логирование
- **pydantic** — Валидация данных

### Требования

- Python 3.10+
- Windows 10/11 (или Linux/Mac)
- Интернет-соединение
- API ключ Kolors

### Производительность

- **Генерация одного изображения**: ~30-60 секунд (зависит от Kolors API)
- **Генерация 3-9 изображений**: ~3-9 минут (последовательно)
- **Сборка сетки**: <1 секунда
- **Общее время**: ~5-10 минут для полной карты

## API Endpoints

### POST `/api/assemble_map`

Создаёт карту желаний.

**Request:**
```json
{
  "wishes": ["желание 1", "желание 2", "желание 3"],
  "format": "phone",
  "selfie_url": "https://..."
}
```

**Response:**
```json
{
  "status": "success",
  "generated_image_urls": ["url1", "url2", "url3"],
  "final_map_url": "file://...",
  "map_b64": "base64_encoded_image"
}
```

## Устранение неполадок

### Бот не отвечает

- Проверьте, что backend запущен на порту 8000
- Проверьте `BOT_TOKEN` в `.env`
- Проверьте логи бота и backend

### Ошибки Kolors API

- Убедитесь, что `KOLORS_API_KEY` установлен в `.env`
- Проверьте баланс API ключа
- Проверьте логи backend для деталей ошибок

### Генерация слишком медленная

- Это нормально для Kolors API (30-60 сек на изображение)
- Система автоматически создаёт placeholder при ошибках
- Убедитесь, что интернет-соединение стабильное

### Ошибки при установке

- Убедитесь, что используете Python 3.10+
- Установите Visual C++ Redistributable для Windows (если нужно)
- Попробуйте установить зависимости по одной

## Лицензия

Проект использует:
- Kolors API (Kling Image) — проверьте лицензию на gen-api.ru
- Все Python библиотеки имеют соответствующие лицензии

## Поддержка

Если возникли проблемы:
1. Проверьте логи backend (`app/api/main.py`) и bot (`app/bot/bot.py`)
2. Убедитесь, что все переменные окружения установлены
3. Проверьте баланс Kolors API ключа
4. Попробуйте перезапустить backend и bot

---

**Версия**: 3.0 (Kolors MVP - только Kolors API, без локальных моделей)
